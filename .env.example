# ── Environment template — placeholders ONLY. Never commit real .env ───────

# Simulation
ARCHIFY_SITE_ID=archify-reference-01
ARCHIFY_DEFAULT_SEED=42
ARCHIFY_PARAMETER_PROFILE=baseline-v1

# Ingest/database (M4)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=crucible
POSTGRES_USER=archify
POSTGRES_PASSWORD=change-me

# MQTT (M4)
MQTT_HOST=localhost
MQTT_PORT=1883

# Read-only API (M6)
API_PORT=8000

# OpenRouter gateway (M7, deferred) — keep the simulator runnable without this
# OPENROUTER_API_KEY=
